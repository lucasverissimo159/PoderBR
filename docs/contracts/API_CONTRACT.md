# API Contract

API responses must use stable field names, documented units and explicit date semantics. Validation errors must be machine-readable and useful to frontend consumers.

Any breaking change requires an ADR or versioning strategy and corresponding consumer updates.
